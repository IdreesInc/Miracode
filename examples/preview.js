function miracode(a, b, c) {
    // The Minecraft font, but monospaced + vectorized
    if (a == b) {
        return "Oh hey, it's like Monocraft";
    } else if (a >= c) {
        return "But instead of pixels, there are vectors";
    } else if (a != b) {
        let c = "Look at those angles";
        let d = "So sharp, so futuristic";
        return c + d;
    } else if (a !== b) {
        return "What am I doing with my life...";
    }
    // TODO: Play more Minecraft
    return "Look, arrows! -> => <-";
}

